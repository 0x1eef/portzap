#!/bin/sh -e

##
# variables
giturl=$1
gitdir=$2

##
# main
if [ -e "${gitdir}/.git" ]; then
    echo "[-] ${gitdir} exists."
    echo "[-] Try 'portzap pull'"
    exit 1
fi
umask u=rwX,g=rwX,o=
set -x
git clone "${giturl}" "${gitdir}"
cd "${gitdir}"
