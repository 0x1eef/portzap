#!/bin/sh
set -e
giturl=$1
gitdir=$2
if [ -e "${gitdir}/.git" ]; then
    echo "${gitdir} already exists."
    echo "Try 'portzap pull' instead."
    exit 1
fi
umask u=rwX,g=rwX,o=
git clone "${giturl}" "${gitdir}"
