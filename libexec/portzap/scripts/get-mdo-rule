#!/bin/sh
set -e

#
# globals
#
user="_portzap"
uid=$(id -u "${user}")
gid=$(/usr/sbin/pw groupshow "${user}" | awk -F: '{print $3}')

#
# main
#
rule="gid=${gid}>uid=${uid},gid=${gid},+gid=${gid};uid=0>uid=${uid},gid=${gid},+gid=${gid}"
echo "${rule}"
